<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Deletion</title>
    <link rel="stylesheet" href="/delete.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="delete-container">
        <div class="delete-card">
            <h2 class="delete-title">Delete Account</h2>
            <p class="delete-subtitle">You are about to permanently delete the account for <strong><%= user.email %></strong> (<%= user.username %>).</p>
            <p class="delete-warning">This action cannot be undone.</p>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-banner">
                    <%= error %>
                </div>
            <% } %>

            <form method="post" action="/user/<%= user.id %>?_method=DELETE">
                <div class="input-group">
                    <label for="password">Enter Password to Confirm</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                <button type="submit" class="delete-btn">Delete This Account</button>
                <a href="/user" class="cancel-link">Cancel</a>
            </form>
        </div>
    </div>
</body>
</html>